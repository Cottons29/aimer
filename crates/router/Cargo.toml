[package]
name = "router"
version = "0.1.0"
edition = "2024"

[dependencies]
widget.workspace = true
attribute.workspace =  true
color.workspace = true
events.workspace = true

url.workspace = true

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
web-sys = { version = "0.3", features = ["Window", "History"] }