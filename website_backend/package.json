{
  "name": "aimer-website-backend",
  "version": "0.2.0",
  "private": true,
  "description": "Cloudflare Worker serving the Aimer blog API from Rust compiled to WebAssembly.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
