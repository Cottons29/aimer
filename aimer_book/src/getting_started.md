# Getting Started

Follow these steps to set up the **Aimer** framework on your machine, create a new project, and run it.

## Prerequisites

Before starting, ensure you have the following installed on your system:

- **Rust**: Ensure you have the latest stable toolchain installed via [`rustup`](https://www.rust-lang.org/tools/install).
- **Platform-Specific Dependencies**:
  - **macOS / iOS**: Xcode and a Mac with Metal-compatible hardware.
  - **Android**: Android NDK.
  - **Web**: `wasm-pack` installed (`cargo install wasm-pack`).

## Installation

The `aimer` CLI tool manages scaffolding, running, and building projects. Since Aimer is evolving, you can install it directly from the repository.

```bash
# Clone the repository
git clone https://github.com/Cottons29/aimer.git

# Enter the directory
cd aimer

# Install the CLI tool
cargo install --path=./aimer
```

## Creating a New Project

Once the CLI is installed, creating a new Aimer application is easy:

```bash
aimer new my_app
```

This will generate a new directory `my_app` with the basic project structure and necessary `Cargo.toml` dependencies.

## Running the App

Navigate to your newly created project directory and start the application using the CLI:

```bash
cd my_app
aimer run
```

If everything is set up correctly, a window should open displaying a "Hello World!" message.

## Your First Widget Tree

Here's an example of the basic entry point for an Aimer application, generated by the CLI:

```rust
#[aimer::main]
pub fn start_app() {
    AimerApp::start(
        Container!(
            child: Text!(
                "Hello World!",
                text_align: TextAlign::MidCenter,
                text_style: TextStyle!(
                    color: Colors::Black,
                )
            )
        )
    );
}
```

The `#[aimer::main]` macro marks the entry point, while `AimerApp::start()` mounts the widget tree. Notice how `Container!` and `Text!` macros make building a UI tree straightforward and readable!
