## Golden Rule


**Using CodeGraph for Understanding Code**, CodeGraph is fast, it uses it. If not, it passes through unchanged. This means CodeGraph is always safe and fastest to use
**Using IDEA for Edit Code**. If IDEA is connected, it uses it. If not, it passes through unchanged. This means IDEA is always safe and fastest to use.

