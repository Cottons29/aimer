## Aimer Command: assemble

### Command usage

- for build the platform bundling like .app .apk .ipa ...etc
- can be build with debug profile and release profile
- must provide the target platform like 'macos', 'android', 'ios' ...etc

**Example:** aimer assemble macos --release # building the macOS bundle in release mode